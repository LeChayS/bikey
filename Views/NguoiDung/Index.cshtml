@model IEnumerable<bikey.Models.NguoiDung>
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Quản lý người dùng";
}
@section Styles {
    <link rel="stylesheet" href="/css/nguoidung/index.css" asp-append-version="true"/>
}

<!-- Top Header -->
<div class="top-header">
    <h1 class="mb-0"><i class="bi bi-people"></i> Quản Lý Người dùng</h1>
    <div>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Thêm người dùng
        </a>
    </div>
</div>

<div class="wrapper">

    <div class="container-fluid">

        <div class="user-table">
            <table class="table table-hover mb-0">
                <thead class="table-primary">
                    <tr>
                        <th>ID</th>
                        <th>Họ tên</th>
                        <th>Email</th>
                        <th>Vai trò</th>
                        <th>Trạng thái</th>
                        <th>Ngày tạo</th>
                        <th >Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @* @foreach (var user in Model)
                    { *@
                        <tr>
                            <td>#1</td>
                            <td>tên</td>
                            <td>gmail@gnail.com</td>
                            <td>
                                <span class="role-badge role-admin">
                                    Quản trị
                                </span>
                            </td>
                            <td>
                                <span class="status-badge status-inactive">
                                    Khóa
                                </span>
                            </td>
                            <td>12/12/1212</td>
                            <td>
                                <a asp-action="Edit" class="btn btn-warning btn-sm btn-action" title="Sửa"><i class="bi bi-pencil"></i></a>
                                <a asp-action="Permission" class="btn btn-info btn-sm btn-action" title="Quản lý quyền"><i class="bi bi-shield-check"></i></a>
                            </td>
                        </tr>

                        <tr>
                            <td>#1</td>
                            <td>tên</td>
                            <td>gmail@gnail.com</td>
                            <td>
                                <span class="role-badge role-staff">
                                    Nhân viên
                                </span>
                            </td>
                            <td>
                                <span class="status-badge status-active">
                                    Khóa
                                </span>
                            </td>
                            <td>12/12/1212</td>
                            <td >
                                <a asp-action="Edit" class="btn btn-warning btn-sm btn-action" title="Sửa"><i class="bi bi-pencil"></i></a>
                                <a asp-action="Permission" class="btn btn-info btn-sm btn-action" title="Quản lý quyền"><i class="bi bi-shield-check"></i></a>
                                @* @if (user.Id != int.Parse(User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier).Value))
                                { *@
                                    @* <form asp-action="Delete" method="post" style="display:inline" onsubmit="return confirm('Bạn có chắc chắn muốn xóa người dùng này?');">
                                        @Html.AntiForgeryToken() *@
                                        <button type="submit" class="btn btn-danger btn-sm btn-action" title="Xóa"><i class="bi bi-trash"></i></button>
                                    @* </form> *@
                                @* } *@
                            </td>
                        </tr>

                        <tr>
                            <td>#1</td>
                            <td>tên</td>
                            <td>gmail@gnail.com</td>
                            <td>
                                <span class="role-badge role-user">
                                    Khách hàng
                                </span>
                            </td>
                            <td>
                                <span class="status-badge status-active">
                                    Khóa
                                </span>
                            </td>
                            <td>12/12/1212</td>
                            <td >
                                <a asp-action="Edit" class="btn btn-warning btn-sm btn-action" title="Sửa"><i class="bi bi-pencil"></i></a>
                                <a asp-action="Permission" class="btn btn-info btn-sm btn-action" title="Quản lý quyền"><i class="bi bi-shield-check"></i></a>
                                @* @if (user.Id != int.Parse(User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier).Value))
                                { *@
                                    @* <form asp-action="Delete" method="post" style="display:inline" onsubmit="return confirm('Bạn có chắc chắn muốn xóa người dùng này?');">
                                        @Html.AntiForgeryToken() *@
                                        <button type="submit" class="btn btn-danger btn-sm btn-action" title="Xóa"><i class="bi bi-trash"></i></button>
                                    @* </form> *@
                                @* } *@
                            </td>
                        </tr>
                    @* } *@
                </tbody>
            </table>
        </div>
        <!-- Thông báo -->
        <div id="userAlert" style="position:fixed;top:20px;right:20px;z-index:9999;min-width:300px;"></div>
    </div>
</div>
